language: smalltalk
sudo: false

smalltalk:
  - Squeak-trunk

baseline:
  - SWTDemo

os:
  - linux

env:
  global:
    - BASELINE_GROUP="TravisCI"
    - PACKAGES="packages"

before_install:
  - printenv
